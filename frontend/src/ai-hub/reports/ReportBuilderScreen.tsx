import { CcReportsScreen } from './CcReportsScreen'

/** Builder is embedded in analytics (canvas parity: Готовые отчёты / Конструктор). */
export function ReportBuilderScreen() {
  return <CcReportsScreen />
}
