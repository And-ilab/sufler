# TLS material for edge nginx (infra/test/nginx.conf).
# Generate: ../gen-self-signed-cert.sh
# Expected files (gitignored): fullchain.pem, privkey.pem
# Never commit private keys — bank/ДИТ CA certs go here on the TEST VM.
