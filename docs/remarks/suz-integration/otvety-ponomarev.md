# Ответы исполнителя на замечания к ТЗ «СУЗ ↔ RAG»

**Автор ответов:** Андрей Пономарев  
**Дата:** 2026-06-12  
**Основание:** 100 комментариев в `TZ-Bitrix-RAG_MIX.docx` (Михайловская Т.Е., Солдатенко Е.П.)  
**Примечание:** комментарии, адресованные **ДИТ** (готовность инфраструктуры, наличие документации Bitrix, проработка с подрядчиком CMS и т.п.), в данном документе **не дублируются** — это зона заказчика. Ниже — ответы по содержанию ТЗ для **КЦ** и **Исполнителя**.  
**Целевой документ:** материал будет включён в **единое ТЗ v1.2**.  
**Правило глоссария:** термины из **ТТ (Прил. 1)** — **дублируем** в ТЗ v1.2 с указанием пункта ТТ; термины, которых в ТТ **нет**, — **дополнительно описываем** в глоссарии ТЗ. Каждый термин — один раз; в тексте разделов — ссылка «см. глоссарий».

---

## 1. Оформление, название, трассировка к ТТ

| № | Замечание (кратко) | Ответ |
|---|-------------------|-------|
| **1** | Название не совпадает с ТТ | Принято. В **едином ТЗ v1.2** заголовок и термины раздела интеграции приведём к **Прил. 1 §1** (СУЗ, Суфлёр, база знаний LLM, ретривер). Интеграцию с СУЗ привяжем к **§7.1** и **§4.5.1.3–4.5.1.6**. Термины «RAG» и «CMS» в заголовке уберём — в ТТ их нет; для СУЗ указано «1С-Битрикс: Система управления сайтом». |
| **2** | Нужны ссылки на пункты ТТ | Согласны. В v1.2 добавим колонку **«ТТ»** в таблицы требований, сценариев INT и критериев приёмки + сводную таблицу «пункт Прил. 1 → раздел ТЗ → сценарий → приёмка» в общем вводном разделе единого документа. |
| **3** | Неверное название проекта | Принято. Поле «Проект» уберём или заменим на наименование из договора: «Работы по кастомизации и внедрению ПО на базе ИИ для ОАО «АСБ Беларусбанк»» (Прил. 1 к договору). |

---

## 2. Глоссарий, термины, роли

| № | Замечание (кратко) | Ответ |
|---|-------------------|-------|
| **11** | Термины — в единственном экземпляре, как в ТТ | Принято. Термины **из ТТ** — **дублируем** в глоссарии v1.2 (со ссылкой на пункт Прил. 1); термины **вне ТТ** (outbox, payload, replay и др.) — **описываем дополнительно**. Каждый термин — **один раз** в глоссарии; в тексте раздела — «см. глоссарий». |
| **12–13** | Определения СУЗ и Суфлёра уже есть в ТТ | Согласны. Текущие определения в тексте не совпадают с ТТ (у «Суфлёра» — «ПО с RAG + LLM…»). В глоссарии v1.2 **продублируем** определения **СУЗ** и **«Суфлёр»** из Прил. 1 §1 дословно (со ссылкой на пункт ТТ). В разделе интеграции — «см. глоссарий». |
| **14** | Роли в §1.1 не совпадают с ТТ | Принято. Роли из Прил. 1 §2.4 **продублируем** в глоссарии v1.2 (со ссылкой на пункт ТТ). Роль «Редактор СУЗ» уберём (см. №17). В разделе — «см. глоссарий». |
| **15** | Дублирование определений по тексту | Принято. Определение — **один раз в глоссарии** (из ТТ — дублируем; новое — описываем). В тексте — только ссылка, без повторов. |
| **16** | В ТТ есть «внутренний пользователь» | Принято. Роль **«Внутренний пользователь»** из Прил. 1 §2.4 **продублируем** в глоссарии v1.2 (со ссылкой на пункт ТТ). Уточним область применения суфлёра для КЦ и внутренних пользователей. |
| **17** | «Редактор СУЗ» — не предмет документа | Согласны. **Кто вносит информацию в СУЗ** — вне предмета интеграционного ТЗ. Оставляем только **Editor_SUZ** как участника диаграммы (технический актор события в CMS), без описания организационной роли. |
| **18** | Оператор КЦ — привести к ТТ | Принято. «Оператор КЦ» приведём к формулировкам Прил. 1 §2.4 и **продублируем** в глоссарии v1.2 (со ссылкой на пункт ТТ). |
| **19** | Что такое АРМ? | АРМ в ТТ не определён — **опишем дополнительно** в глоссарии v1.2 (автоматизированное рабочее место оператора: модули «Суфлёр», онлайн-чат, телефония). В разделе — «см. глоссарий». |
| **10, 17** | «Production-индекс» — ввести термин | Принято. Термин «рабочий индекс» (production-индекс) **в ТТ не определён** — опишем дополнительно в глоссарии v1.2 (поисковый индекс суфлёра для опубликованных статей КЦ). В разделе — «см. глоссарий». |
| **8** | Неверно «Подразделение по ИБ» | Принято. В ТТ (§8–9) **нет термина «ЦК»** и **нет названия подразделения** — есть «**требования информационной безопасности**»; аббревиатура **ИБ** (§8.12, §9.1.9). Строку «Подразделение по…» уберём. В глоссарии v1.2: **ИБ** — требования §8–9 Прил. 1. Не путаем с **КЦ** (Контакт-центр). |
| **65, 68** | «Битрикс» / «Bitrix» — разнобой | Принято. В русскоязычном тексте — **«1С-Битрикс»**; **Bitrix** — только в идентификаторах кода/API (`bank.sufler.sync`, REST). |
| **43** | «1С-Битрикс: Управление сайтом» — термин из ТТ | Принято. Используем определение из Прил. 1 §1: «1С-Битрикс: **Система управления сайтом**» (не «CMS»). |

---

## 3. Контент СУЗ: что попадает в RAG

| № | Замечание (кратко) | Ответ |
|---|-------------------|-------|
| **20 (id=28)** | Инфоблок — «используется в интеграции?» | **Да, используется:** в §6 (payload, INT-10), матрице BTX и маппинге полей CMS. В ТТ не определён — глоссарий v1.2. |
| **22 (id=30)** | Элемент / статья — «используется?» | **Да, используется:** в payload (`article_id`), сценариях INT и критериях приёмки §4.6. |
| **23 (id=31)** | Раздел — «используется?» | **Да, используется:** в payload (`section_id`), правилах области индексации для КЦ (BTX-4) и фильтрах индекса. |
| **24 (id=32)** | Черновик — «используется?» | **Да, используется:** в payload (`status=draft`), сценариях INT; черновик передаётся в обмене, но **не попадает в рабочий RAG-индекс**. Глоссарий v1.2. |
| **25 (id=33)** | Публикация — триггер (не «используется?», но тот же ряд глоссария) | **Да, используется:** `status=published`, `event_type`, INT (`article.published`), BTX-4, §4.6. Триггер — OnAfterIBlockElement* + `status=published`. |
| **26 (id=34)** | permalink — «что значит определение?» | **Да, используется:** `permalink` в payload §6.2, INT, подсказки оператору; п. **4.3.1.8 ТТ** — ссылка на статью/раздел СУЗ. **По Солдатенко Е.П. (id=180):** «При изменении контента ссылка не меняется, ссылка меняется при изменении настраек статей, а они почти не меняются.» |
| **27 (id=35)** | visibility_scope — «где в ТТ?» | **Да, используется:** `visibility_scope` в payload §6.2, BTX-4, Vector Store; вычисляет модуль sync (разделы, права). Отдельного свойства «доступно КЦ» **нет** (Солдатенко Е.П., id=179: «Признака доступности операторам нет, все настраивается правами»). |
| **—** | **Версия статьи** (§1.3) — в MIX **нет комментария** на строке | Готовый ответ: **Да, используется** — `version_id`, `version_number`, `is_current` в payload §6, INT (`article.version_published`), §4.6; в RAG только `is_current=true`. См. также id=167. |
| **21, 44, 60, 75 (id=154)** | Таблицы, тултипы, спойлеры — попадают ли в RAG? | **Да.** Это элементы HTML внутри текста статьи СУЗ. Модуль sync передаёт их **вместе с текстом** в поле `body_html` webhook — не как отдельные объекты CMS. Отдельной записи «таблица»/«спойлер» в payload нет; суфлёр извлекает содержимое из HTML при индексации. Термин **«сериализация»** (преобразование данных статьи в JSON payload) — опишем дополнительно в глоссарии v1.2. |
| **44, 60** | «Стандартные инфоблоки» — откуда? | Из модели хранения СУЗ на 1С-Битрикс (элементы инфоблока). Детальная структура полей и видов контента — в **регламенте BTX-5** (коды свойств, whitelist разделов), согласуемом с владельцем СУЗ. |
| **76** | Файлы-вложения — по ТТ не используются | Принято. В v1.2 зафиксируем: **файлы-вложения в RAG v1 не индексируются** (п. ТТ). Строку матрицы №4 сформулируем как «не входит в MVP / v1.2», без передачи URL файлов суфлёру. |
| **60** | Какие сущности СУЗ — источник для RAG | **Статьи** (элементы инфоблока в области индексации для КЦ) — основной источник; **метаданные** — раздел, свойства, статус, версия, permalink. **Разделы** — для навигации и фильтров, не как отдельный текст для ответов. Вложенная разметка — в составе текста статьи. |

---

## 4. Бизнес-правила и формулировки

| № | Замечание (кратко) | Ответ |
|---|-------------------|-------|
| **46, 73** | Бизнес-правила — из ТТ | Принято. В v1.2 **не дублируем** полный текст правил КЦ; даём **ссылки на пункты ТТ** + таблицу «правило ТТ → реализация в обмене (поле payload / event_type)». |
| **47** | «Оператор только из СУЗ» — неверно | **Согласны.** Корректная формулировка: **источником знаний для подсказок суфлёра является СУЗ**; оператор может использовать иные системы вне суфлёра. Исправим в v1.2. |
| **48** | Ссылка на статью в подсказках — есть в ТТ | Принято. Добавим **ссылку на пункт ТТ** и опишем: в payload передаётся `permalink`; АРМ суфлёра отображает ссылку оператору (раздел «Суфлёр» единого ТЗ). |
| **49** | Запрет ссылок клиенту — общее правило ТТ | Согласны. Это **общее требование ТТ** к модулю «Суфлёр», не специфика обмена СУЗ↔Bitrix. В интеграционном разделе — перекрёстная ссылка на общий раздел v1.2. |
| **29–30** | «Источник истины для ответов КЦ» — неверный акцент | Принято. Формулировка: **СУЗ — авторитетный источник контента для индексации и подсказок суфлёра** (п. ТТ о базе знаний LLM). Уточнение «для ответов КЦ» уберём. |
| **59** | Повтор «источник истины» | Принято. Оставим **один раз** в общем глоссарии v1.2. |
| **50** | Статьи меняются постоянно | Принято. Заменим «несколько раз в день» на **«регулярно, в течение рабочего дня»** / формулировку ТТ. |
| **51** | Изменения не только точечные | Согласны. Укажем: возможны **точечные правки, смена раздела, перенос, новые разделы, переименования** — модель событий и версий это покрывает (`article.updated`, `article.version_published`, смена `section_id`). |
| **45** | П. 7.1 Прил. 1 — должна быть именно интеграция | Согласны. Раздел v1.2 — **детализация п. 7.1**: контракт обмена, события, payload, приёмка **интеграционного контура**; без подмены общими требованиями к суфлёру. |

---

## 5. Модель интеграции, webhook, SLA

| № | Замечание (кратко) | Ответ |
|---|-------------------|-------|
| **4, 52** | Описана модель «webhook + GET» — что за данные? GET на диаграмме? | **§3.7–3.8** — описаны **модель A** (короткий webhook + GET INT-06) и **модель B** (полный текст в POST) с обоснованием выбора B. В актуальной версии (**модель B**) webhook содержит **метаданные и полный текст** (`body_html`, `body_plain`); **отдельный GET после каждого события не используется**. Перечень полей — §6.2; GET — только **INT-10** (первичная загрузка) и опционально **INT-06** / fallback. |
| **57 (id=73)** | «GET статьи» — каким? кто создаёт? | **§3.7–3.8.** **Модель B (выбранная):** GET после webhook **не нужен** — текст в POST (`body_html`/`body_plain`). **Модель A:** Bitrix — webhook; суфлёр — GET INT-06. **`iblock.element.get`** — **штатный REST платформы 1С-Битрикс** [Штатно], Исполнитель **не создаёт**; в проекте — только **INT-10** и опц. **INT-06** (fallback / модель A). Черновик «GET после webhook» — устаревшая модель A, не INT-01…05, 08. |
| **25, 54, 63, 84** | Какой триггер публикации? | **Триггер** — бизнес-событие «статья опубликована для области индексации КЦ», фиксируемое модулем sync при срабатывании **[Штатно]** `OnAfterIBlockElementAdd/Update` **после** применения правил банка (`status=published`, доступ по правам/разделам). Конкретная привязка к кнопке/статусу в UI СУЗ — в **регламенте BTX-4** (согласование с владельцем СУЗ). |
| **78** | «Push-уведомление» — какой процесс? | Опишем пошагово в v1.2: событие CMS → запись outbox → HTTP POST на endpoint суфлёра → ответ `202` → индексация. Без отдельного «уведомления» оператору — это фоновый обмен систем. |
| **37, 86 (id=45, 165)** | SLA 1–2 мин — слишком долго; в ТТ «онлайн» | Согласны. Ориентир «1–2 мин» в черновике уберём. **ТТ §4.5.1.3** — обновление базы знаний в **режиме реального времени**; **§4.1.5** — режим онлайн. Числовой порог «СУЗ → индекс» в ТТ **не задан**. **«< 2 мин»** в §4.6 — **только верхняя граница приёмочного теста** доставки webhook. Целевой режим: webhook сразу при сохранении. |
| **56, 58, 87 (id=72, 74, 166)** | Скорость очереди / обновления / подсказки оператору | **Outbox:** webhook сразу при сохранении; retry ≤1 мин при сбое. **ТТ не задаёт** сквозных секунд «публикация → подсказка». **§4.5.1.3** — режим реального времени. **После обновления индекса** выдача подсказки в диалоге: **§4.3.1.4** — ≤1–2 с (телефония); **§4.3.2.3** — ≤2 с (онлайн-чат); **§3.4** — ответ LLM 1–2 с. «< 2 мин» — только тест §4.6, не SLA подсказки. |
| **33** | Чей приёмник (идемпотентность)? | **Приёмник — сервис Sufler_Ingest** (контур исполнителя). Повтор одного `event_id` не должен дублировать индекс. |
| **34** | Outbox — проработано? | Механизм — **новая доработка CMS** (модуль `bank.sufler.sync`, BTX-3). Требования описаны в ТЗ; реализация — на стороне CMS в рамках BTX. |
| **36** | Где хранится курсор? | **Курсор** — на стороне суфлёра (INT-09/reconcile) и/или в **outbox/journal** модуля sync (метка последнего выданного события для `GET /changes`). |
| **35** | Webhook через API Gateway — есть ли у СУЗ? | Канал — **внутренний HTTP POST** в контуре банка (URL согласует заказчик). Схема «через API Gateway» — типовой вариант; финальная топология — по архитектуре банка (не детализируем в ТЗ КЦ). |
| **66** | «Разово ок через REST» — описка? | Да, «Разово ок» — **неудачное сокращение** в таблице. Имелось в виду: **разовая первичная загрузка (INT-10)** допустима через штатный REST `iblock.element.list`; для **оперативной синхронизации** REST не заменяет webhook (модель B). В v1.2: «Разовая полная выгрузка (INT-10) — через REST [Штатно]; оперативные изменения — webhook (INT-01…08)». |
| **90** | «Сбой сети» — какой? | Имеется в виду **недоступность endpoint суфлёра** (HTTP 503/timeout) во **внутренней сети банка** между CMS и контуром суфлёра. Сценарий INT-08: outbox → retry → досылка. |
| **91** | «50 последовательных update» — откуда? | **Нагрузочный приёмочный тест** (п. 7 таблицы §4.6): имитация серии правок в СУЗ; проверка, что outbox **не теряет события** и суфлёр обрабатывает все `event_id`. Не прогноз производственной нагрузки. |

---

## 6. Статусы, область индексации КЦ, поля СУЗ

| № | Замечание (кратко) | Ответ |
|---|-------------------|-------|
| **70, 97 (id=86, 178)** | Статус в СУЗ — откуда? | Отдельного поля «status» в **1С-Битрикс: Система управления сайтом (СУЗ)** нет — нормализованный **`status`** (`draft` / `published` / `archived`) формирует **модуль sync** по правилам **BTX-4** (маппинг из сырого ACTIVE и др.). **По уточнению Солдатенко Е.П. (id=178):** «Черновик используется КЦ, я точно не помню, но скорее всего речь идет о активности статьи, ставится галка.» |
| **79, 89** | Как используется статус черновик/опубликовано/архив? | **`draft`** — webhook может уйти, RAG **не обновляет** production-индекс. **`published`** — индексация/обновление. **`archived`/`unpublished`** — **soft delete** (исключение из retrieval). **`deleted`** — hard delete. |
| **27, 80, 98 (id=35, 159, 179)** | `visibility_scope` — где в ТТ? | Отдельного свойства «доступно КЦ» **нет** (Солдатенко Е.П., id=179: «Признака доступности операторам нет, все настраивается правами») — доступ через **права групп и разделы** инфоблока в **1С-Битрикс: Система управления сайтом (СУЗ)**. В v1.2: поле «область видимости» (`visibility_scope` в payload) и принадлежность к **области индексации для КЦ** **вычисляет модуль `bank.sufler.sync`** (BTX-4) по whitelist разделов (BTX-1) и правилам банка; отдельного свойства инфоблока не выдумываем. |
| **67** | Где фильтр «только КЦ»? | **Не отдельный переключатель в СУЗ.** Штатно в 1С-Битрикс: разделы, `ACTIVE`, права групп, пользовательские свойства (REST/админка); готового флага «только для КЦ» нет. **[Доработка]** `bank.sufler.sync` (BTX-4): в `OnAfterIBlockElement*` вычисляет `visibility_scope` и `status=published`; статьи вне **области индексации для КЦ** — без publish-событий (строка 13 §4.2). **Настройка:** (1) админка СУЗ — разделы и права; (2) BTX-1 — `IBLOCK_ID`, whitelist `section_id`; (3) код BTX-4. **Суфлёр:** фильтр при индексации/retrieval по `status=published` и `visibility_scope` (`kc_operator`). |
| **26, 99 (id=34, 180)** | Что такое permalink? | **Постоянная ссылка** на статью в СУЗ для оператора (открытие из подсказки). **По Солдатенко Е.П. (id=180):** «При изменении контента ссылка не меняется, ссылка меняется при изменении настраек статей, а они почти не меняются.» Передаётся в payload полем `permalink`. |
| **88** | Если версия v10? | Поддерживается **любой номер версии** (`version_number`, `version_id`); в RAG одновременно **только `is_current=true`**. Сценарий приёмки с v2 — **пример**, не ограничение. |

---

## 7. RAG, индексация, разметка

| № | Замечание (кратко) | Ответ |
|---|-------------------|-------|
| **31** | Черновик чего попадает в RAG? | **Черновик статьи в CMS** (`status=draft`) — **не попадает** в production-индекс. Черновики Битрикса без публикации не индексируются. |
| **38** | Vector Store — какие данные? | Векторы чанков + метаданные: `article_id`, `version_id`, `chunk_index`, `section_id`, `status`, hash/checksum — для фильтрации и версионности. Деталь — в разделе «Суфлёр» единого ТЗ. |
| **9 (id=16)** | Replay — кто и что? | **Согласны:** ручной replay — не основной способ доставки. **Штатно — автоматически:** событие → outbox → HTTP POST; при сбое — retry (BTX-3) и досылка failed агентом (BTX-7), без участия человека. **Replay вручную — только резерв (BTX-8):** если автоповторы исчерпаны или нужна разовая досылка по инциденту — кнопка в журнале модуля `bank.sufler.sync`, тот же `event_id`/payload, **не правка статьи**. В глоссарии v1.2: «Replay — ручная досылка (fallback)»; термины «outbox», «event_id», «replay» **в ТТ не определены** — опишем дополнительно. |
| **41 (id=49), 72 (id=88), 74, 77** | ТТ 4.5.1.3–4.5.1.6 — должен делать исполнитель | **Согласны.** По Прил. 1: **4.5.1.3** — обновление базы знаний LLM из СУЗ в реальном времени (Исполнитель: INT, индексация; Bitrix: события и payload, п. 7.1, §4.1.5); **4.5.1.4** — оповещения об изменениях для проверки администраторами (Исполнитель, раздел «Суфлёр»); **4.5.1.5** — логирование обновления базы знаний LLM (Исполнитель); **4.5.1.6** — разметка СУЗ для LLM разработчиком по согласованию с Заказчиком (нормализация, чанкинг, эмбеддинги — Исполнитель, не метаданные CMS). Строка «Инфраструктура заказчика» — канал (endpoint, сеть, TLS), зона Заказчика/ДИТ; Исполнитель размещает ПО на ней. §4 интеграции — только минимум для Bitrix (BTX-1…10). В v1.2 — таблица «п. ТТ → зона Исполнителя / зона Bitrix / зона Заказчика». |
| **74 (id=153), 77 (id=156)** | Разметка — что это, кто делает | **П. 4.5.1.6 ТТ:** «разметка СУЗ для работы с LLM» — **не** создание метаданных в СУЗ (инфоблок, разделы, свойства, HTML уже есть), а **методика подготовки контента для базы знаний LLM** (нормализация HTML, чанки, эмбеддинги, правила метаданных индекса) — **Исполнитель**, по согласованию с Заказчиком. Строка BTX-5 про «регламент для суфлёра» (ID инфоблока, коды свойств) — **техспецификация интеграции**, не п. 4.5.1.6. Маппинг свойств в payload (BTX-4) — перенос существующих метаданных, не разметка для LLM. |
| **96 (id=177)** | Сервисная учётка для чтения инфоблока | **Солдатенко Е.П. (id=177):** «Сделать можно, но для чтения инфоблоков пользователь не нужен. Авторизованный пользователь КЦ использует для получения статистики, тут в сборе статистики нет необходимости.» Для **webhook-модели B** отдельная учётка для **оперативного** чтения **не обязательна** (текст в POST). REST read — для **INT-10** / опционального fallback; требование уточним в BTX-5. |
| **100 (id=181)** | Тестовая среда | **Солдатенко Е.П. (id=181):** «Тестовая среда это копия СУЗ (актуальность статей - пол года назад).» Дополнительно: **отдельный URL webhook** на тестовый контур суфлёра (BTX-20 / строка 20 матрицы). |

---

## 8. ИБ, приёмка, границы документа

| № | Замечание (кратко) | Ответ |
|---|-------------------|-------|
| **81, 95 (id=160, 174)** | Требования ИБ / «ЦК смотрели?» | В ТТ (§8–9) — «**требования информационной безопасности**» (**ИБ**); термина **«ЦК» в ТТ нет** (не путать с **КЦ** — Контакт-центр). В v1.2 — отсылка к §8–9 ТТ и матрице мер (TLS, HMAC-SHA256/mTLS, allowlist IP). Заключение/согласование по требованиям ИБ — **предоставляет Заказчик**. |
| **83 (id=162)** | Критерии приёмки — только обмен | **Согласны.** §4.6 — **только приёмка интеграционного обмена** СУЗ ↔ суфлёр: webhook/outbox (HTTP 202, журнал pending/sent/failed), payload BTX-10, идемпотентность INT. Строки «production-индекс», «soft delete» — **результат обмена**, без UI и качества подсказок. Полный пул ТТ (§4.3, §4.5, §4.5.1.3–4.5.1.6, окно оператора) — **отдельная приёмка КЦ**, раздел «Суфлёр» v1.2. В §4.6 переработаем таблицу и добавим перекрёстные ссылки на ТТ. |
| **85** | Приёмка: разные типы контента, большие разделы | Расширим **матрицу приёмки**: статья с таблицами/спойлерами, перенос между разделами, крупный новый раздел (пример — СДБО). Критерий — корректный `body_html` в payload и отражение в индексе. |
| **93 (id=172)** | Как пользователь видит успех интеграции? | Отдельного «окна успеха» для редактора **нет**. **Основной контроль — не штатный «Журнал событий» Bitrix**, а **админ-страница модуля `bank.sufler.sync`** (BTX-8, доработка): журнал outbox со статусами `pending`/`sent`/`failed`, текстом ошибки, `event_id`, кнопка **replay**. На приёмке §4.6: HTTP **202**, в журнале **sent**, payload по schema (BTX-10). «Настройки → Журнал событий» Bitrix — **дополнительно** (срабатывание `OnAfterIBlockElement*`), но для приёмки интеграции — **журнал модуля**. **Оператор КЦ** — **косвенно**: актуальная подсказка в АРМ суфлёра; UI оператора — отдельная приёмка (раздел «Суфлёр»). |
| **94** | Приёмка результата в суфлёре — зона КЦ | **Согласны.** Сценарии INT проверяют **обмен**; сценарии UC оператора (окно, кнопки, «Неверно», ошибки) — в **разделе «Суфлёр»** единого ТЗ v1.2 с отдельной приёмкой КЦ. |
| **7** | Описание штатных методов Bitrix — не зона КЦ | Для **[Штатно]** в v1.2 — **ссылки на dev.1c-bitrix.ru** без дублирования; детальная реализация — документация CMS / зона CMS. КЦ проверяет **результат для оператора**, не REST Bitrix. |

---

## 9. Комментарии без ответа (адресованы ДИТ)

Следующие номера **не требуют ответа исполнителя** — вопросы к подразделению заказчика (ДИТ / CMS):  
**5, 6, 7, 32, 34, 35, 39, 40, 42, 53, 55, 61, 62, 63, 64, 69, 71, 82, 92**, а также часть **№94** про готовность ДИТ принять экспертизу интеграции.

---

## 10. Сводка по единому ТЗ v1.2

| Блок | Действие в v1.2 |
|------|-----------------|
| Структура | Раздел «Интеграция СУЗ ↔ суфлёр» внутри единого документа; общий глоссарий и роли |
| Модель | **B** — webhook с полным телом; **§3.7–3.8** — модели A и B, обоснование; диаграммы без «короткий webhook + GET» |
| ТТ | Колонка «ТТ», перекрёстные ссылки; без дублирования общих правил КЦ |
| Контент | Явный перечень сущностей RAG; HTML-разметка в `body_html`; файлы v1 — нет |
| SLA | «Онлайн» по ТТ + уточнённые пороги приёмочных тестов |
| Приёмка | §4.6 — только интеграция; UI/подсказки — раздел «Суфлёр» |
| СУЗ (факты) | Учтены ответы **Солдатенко Е.П.** (09.06.2026) по статусам, правам, permalink, тестовой среде |

---

**Word с ответами в комментариях:** [TZ-Bitrix-RAG +-ответы-исполнителя.docx](./TZ-Bitrix-RAG%20%2B-%D0%BE%D1%82%D0%B2%D0%B5%D1%82%D1%8B-%D0%B8%D1%81%D0%BF%D0%BE%D0%BB%D0%BD%D0%B8%D1%82%D0%B5%D0%BB%D1%8F.docx) · генерация: [add_comment_replies.py](./add_comment_replies.py)

*Текст документа `TZ-Bitrix-RAG_MIX.docx` исполнителем не изменялся — добавлены только ответы в комментариях.*
