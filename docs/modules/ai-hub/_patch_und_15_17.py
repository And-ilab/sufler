"""Patch tz-unified-v1.4.md: FR-UND-15/16/17 per TT §4.2.1.15–17."""
from pathlib import Path

path = Path(__file__).parent / "tz-unified-v1.4.md"
text = path.read_text(encoding="utf-8")

replacements = [
    # FR-UND-08: journal ref
    (
        "**Выход:** журнал обучения (FR-UND-15): события `training.started`",
        "**Выход:** журнал обучения ([II.2.6](#ii26-механика-эталонов-обучающей-выборки-qu-4214)): события `training.started`",
    ),
    # FR-UND-09: moderation belongs to §4.2.1.9, not FR-UND-16
    (
        "`auto_with_confirmation` (**по умолчанию**) — черновик эталона создаётся автоматически со статусом `pending_review`, **без** попадания в активный индекс QU до утверждения (FR-UND-16); `auto`",
        "`auto_with_confirmation` (**по умолчанию**) — черновик эталона создаётся автоматически со статусом `pending_review`, **без** попадания в активный индекс QU до утверждения **Администратором базы знаний LLM** с обязательным комментарием (очередь «На модерации», UC-UND-05); `auto`",
    ),
    (
        "**Приёмка:** UC-UND-03. | 4.2.1.9 | готово |",
        "**Приёмка:** UC-UND-05. | 4.2.1.9 | готово |",
    ),
    # FR-UND-14: cross-link §4.2.1.15
    (
        "| FR-UND-14 | Должна быть возможность уточнения вопроса в случае, когда релевантности найденных документов недостаточно для прямого ответа, при этом должен предоставляться пользователю список вариантов ответа для выбора. | **Вход:** оценка релевантности найденных документов ниже порога. **Механизм реализации:** ветка сценария «уточняющие вопросы» (промпт `sufler_cc`). **Выход:** клиенту или оператору предлагаются кнопки-варианты или эскалация. **Приёмка:** UC-UND-02. | 4.2.1.14 | готово |",
        "| FR-UND-14 | Должна быть возможность уточнения вопроса в случае, когда релевантности найденных документов недостаточно для прямого ответа, при этом должен предоставляться пользователю список вариантов ответа для выбора. | **Вход:** `min_relevance_clarify` ≤ `max(relevance_score)` **<** `min_relevance` (FR-UND-13, FR-UND-15) **или** семантическая неоднозначность (top-2 с Δ ≤ 5 п.п.). **Механизм реализации:** retriever QU (FR-UND-06) → ветка «уточняющие вопросы» диалогового сценария (`sufler_cc`, [FR-SCR-08](#ii351-редактор-диалоговых-сценариев)); формирование ≥2 вариантов из top-k документов `cc_production`. **Выход:** (телефония) оператору — варианты **без** автоответа клиенту; (онлайн-чат) клиенту — кнопки-варианты **только** при `max(relevance_score)` ≥ `min_relevance_clarify` (FR-UND-15); иначе — эскалация оператору ([FR-SCR-08](#ii351-редактор-диалоговых-сценариев)). **Приёмка:** UC-UND-02, UC-UND-03, SUF-T-16. | 4.2.1.14 | готово |",
    ),
    # FR-UND-15 per TT
    (
        "| FR-UND-15 | Процесс обучения модуля понимания: журнал итераций, откат версии обучающей выборки | **Вход:** завершена очередная итерация обучения модуля понимания. **Механизм реализации:** версионируемый журнал обучения с возможностью отката выборки. **Выход:** доступны история итераций и откат выборки. **Приёмка:** UC-UND-01. | 4.2.1.15 | готово |",
        "| FR-UND-15 | Администратор базы знаний LLM должен иметь возможность указать минимальную релевантность для вывода списка вариантов уточняющих вопросов клиенту. | **Вход:** **Администратор базы знаний LLM** (§2.4 п.2) задаёт порог в Hub → «Модуль понимания» → «Пороги релевантности». **Механизм реализации:** конфигурация `min_relevance_clarify` (0–100 %) для каждой [**KB**](#i3-глоссарий) `cc_production` — **отдельно** от `min_relevance` (FR-UND-13); валидация UI: `min_relevance_clarify` ≤ `min_relevance`; при runtime — список кнопок-вариантов клиенту в онлайн-чате формируется только если `min_relevance_clarify` ≤ `max(relevance_score)` **<** `min_relevance` (связь с уточнением §4.2.1.14, FR-UND-14, UC-UND-02). **Выход:** сохранённый порог; в preview (FR-UND-12) — индикация «уточнение клиенту: доступно / недоступно» для тестового запроса. **Приёмка:** UC-UND-03, SUF-T-16. | 4.2.1.15 | готово |",
    ),
    # FR-UND-16 per TT
    (
        "| FR-UND-16 | Ручная валидация новых примеров Администратором базы знаний LLM с комментарием | **Вход:** автоматически предложен или создан новый пример вопроса (FR-UND-09, режим `auto_with_confirmation` / `suggest`). **Механизм реализации:** очередь модерации Hub → «Модуль понимания» → «На модерации» с полем комментария **Администратора базы знаний LLM**; утверждение → активация эталона + автозапуск retrain (FR-UND-08); отклонение → soft-delete без влияния на индекс QU. **Выход:** Администратор базы знаний LLM утверждает или отклоняет пример. **Приёмка:** UC-UND-03. | 4.2.1.16 | готово |",
        "| FR-UND-16 | Должна быть возможность включить проверку и настройку действий в случае одинаковых реплик пользователя, поступивших в диалоге несколько раз подряд. | **Вход:** в активном диалоге (телефония — реплика клиента после ASR; онлайн-чат — сообщение клиента) поступает реплика, **нормализованно идентичная** предыдущей реплике того же участника подряд (trim, регистр); `duplicate_replica_check_enabled = true` в Hub → «Модуль понимания» → «Поведение диалога». **Механизм реализации:** сравнение с буфером последней обработанной реплики сессии (`session_id`); настраиваемое **действие** (**Администратор базы знаний LLM**): `skip_processing` (**по умолчанию**) — не запускать повторный retriever/LLM; `repeat_last_hint` — повторить последнюю подсказку оператору; `ack_only` — краткое подтверждение без подсказки; `escalate_operator` — уведомление оператору о зацикливании; журнал аудита §9: `duplicate_replica_detected`, действие, `session_id`. **Выход:** применено настроенное действие; оператор видит пометку «повтор реплики» без дублирования карточек подсказок. **Приёмка:** UC-UND-04, SUF-T-17. | 4.2.1.16 | готово |",
    ),
    # FR-UND-17 per TT
    (
        "| FR-UND-17 | Эскалация оператору при невозможности автоматического ответа | **Вход:** модуль понимания не может сформировать автоматический ответ. **Механизм реализации:** эскалация в канал оператора. **Выход:** оператор получает уведомление. **Приёмка:** UC-UND-02. | 4.2.1.17 | готово |",
        "| FR-UND-17 | Разработчиком должны быть обеспечены регулярные обновления и улучшения модуля понимания в течение гарантийного периода и технической поддержки. | **Вход:** наступил гарантийный период (1 год с даты акта приёмки, [Прил.1 §условия гарантийной поддержки]) или период техподдержки (2 года после гарантии). **Механизм реализации:** **Исполнитель** поставляет патчи/релизы компонента QU (модели embedding, retriever, словари RU/EN) по регламенту сопровождения [VII.1](#vii1-состав-и-содержание-работ); журнал изменений (release notes) с перечнем улучшений QU; регрессионное тестирование SUF-T-12, SUF-T-16, SUF-T-17 после каждого обновления QU; устранение дефектов QU — в сроки гарантийной поддержки ТТ. **Выход:** установленное обновление QU без деградации p95 подсказок (FR-SUF-06); зафиксированная версия в Hub admin → «Модуль понимания» → «О системе». **Приёмка:** [VII.2](#vii2-контроль-и-приёмка) этапы 6–9; акт сопровождения. | 4.2.1.17 | готово |",
    ),
    # UC section header
    (
        "**[Прил.1 §4.2.1.8–14]: UC-UND-01…02**",
        "**[Прил.1 §4.2.1.8–17]: UC-UND-01…05**",
    ),
    # UC-UND-01
    (
        "(2) сохранение порогов `min_relevance`; (3) preview поиска",
        "(2) сохранение порогов `min_relevance` и `min_relevance_clarify` (FR-UND-15); (3) preview поиска",
    ),
    (
        "**Выход:** журнал обучения (версии выборки, откат — FR-UND-15) и актуальный статус индекса. **Приёмка:** UC-UND-01 на стенде с тестовым Bitrix: создание эталона → preview → изменение СУЗ → автообучение. | 4.2.1.4, 4.2.1.8–4.2.1.13 | готово |",
        "**Выход:** журнал обучения (версии выборки, откат — [II.2.6](#ii26-механика-эталонов-обучающей-выборки-qu-4214)) и актуальный статус индекса. **Приёмка:** UC-UND-01 на стенде с тестовым Bitrix: создание эталона → preview → изменение СУЗ → автообучение. | 4.2.1.4, 4.2.1.8–4.2.1.13, 4.2.1.15 | готово |",
    ),
    # UC-UND-02: fix escalation ref, add FR-UND-15
    (
        "при низкой релевантности — ветка «уточняющие вопросы» (`sufler_cc`, FR-UND-14) или эскалация (FR-UND-17). **Выход:** (телефония/чат) оператору — уточняющие варианты **без** автоответа клиенту; (онлайн-чат) клиенту — кнопки-варианты по политике КЦ;",
        "при низкой релевантности — ветка «уточняющие вопросы» (`sufler_cc`, FR-UND-14); кнопки клиенту — только при `max(relevance_score)` ≥ `min_relevance_clarify` (FR-UND-15); иначе — эскалация оператору ([FR-SCR-08](#ii351-редактор-диалоговых-сценариев)). **Выход:** (телефония/чат) оператору — уточняющие варианты **без** автоответа клиенту; (онлайн-чат) клиенту — кнопки-варианты по политике КЦ при выполнении порога FR-UND-15;",
    ),
    (
        "| 4.2.1.6, 4.2.1.14 | готово |\n\n### II.2.5.",
        "| 4.2.1.6, 4.2.1.14, 4.2.1.15 | готово |\n| UC-UND-03 | Администратор базы знаний LLM настраивает порог релевантности для уточняющих вопросов клиенту | **Вход:** Администратор базы знаний LLM открывает Hub → «Модуль понимания» → «Пороги релевантности». **Механизм реализации:** задаёт `min_relevance_clarify` (FR-UND-15), отдельно от `min_relevance` (FR-UND-13); тестовый запрос в preview (FR-UND-12). **Выход:** при запросе с `min_relevance_clarify` ≤ max % < `min_relevance` — в тест-диалоге/онлайн-чате отображаются ≥2 варианта уточнения (FR-UND-14); при max % < `min_relevance_clarify` — варианты клиенту не выводятся, UC-UND-02. **Приёмка:** (1) `min_relevance_clarify` > `min_relevance` — валидация UI отклоняет; (2) max %=45 при `min_relevance`=60, `min_relevance_clarify`=40 → уточнение показано; (3) max %=35 → уточнение скрыто. | 4.2.1.15 | готово |\n| UC-UND-04 | Обработка одинаковых реплик пользователя подряд в диалоге | **Вход:** клиент в телефонии или онлайн-чате дважды подряд отправляет идентичную реплику; `duplicate_replica_check_enabled=true` (FR-UND-16). **Механизм реализации:** детектор consecutive duplicate; применение настроенного действия (`skip_processing` / `repeat_last_hint` / `ack_only` / `escalate_operator`). **Выход:** вторая реплика не порождает дублирующий retriever/подсказки (при `skip_processing`); в журнале — `duplicate_replica_detected`. **Приёмка:** (1) два одинаковых сообщения в онлайн-чате → одна волна подсказок; (2) смена действия на `escalate_operator` → оператор получает уведомление; (3) отключение проверки → обе реплики обрабатываются независимо. | 4.2.1.16 | готово |\n| UC-UND-05 | Ручная валидация автодобавленных формулировок Администратором базы знаний LLM (§4.2.1.9) | **Вход:** система создала черновик эталона `pending_review` (FR-UND-09, политика `auto_with_confirmation` / `suggest`); Администратор базы знаний LLM открывает Hub → «Модуль понимания» → «На модерации». **Механизм реализации:** очередь модерации с **обязательным** полем комментария; утверждение → активация эталона в [II.2.6](#ii26-механика-эталонов-обучающей-выборки-qu-4214) + автозапуск retrain (FR-UND-08); отклонение → soft-delete без влияния на индекс QU. **Выход:** пример в активной выборке или отклонён с комментарием в журнале. **Приёмка:** (1) production-диалог → черновик в очереди; (2) утверждение с комментарием → эталон в preview (FR-UND-12); (3) отклонение → запись не в активном индексе. | 4.2.1.9 | готово |\n\n### II.2.5.",
    ),
    # II.2.6 cross-links
    (
        "очередь модерации (FR-UND-16); приёмка UC-UND-03",
        "очередь модерации (UC-UND-05); приёмка UC-UND-05",
    ),
    (
        "Новая версия записи в журнале (FR-UND-15); переиндексация",
        "Новая версия записи в журнале обучения; переиндексация",
    ),
    (
        "Журнал итераций обучения; откат к предыдущей версии выборки (FR-UND-15)",
        "Журнал итераций обучения; откат к предыдущей версии выборки (**только Администратор базы знаний LLM**; аудит §9)",
    ),
    # II.7 SUF-T rows
    (
        "| SUF-T-15 | Запрос вне СУЗ → оператору нет подсказки / низкий %; без автоответа клиенту | FR-CC-06, FR-UND-18 | готово |",
        "| SUF-T-15 | Запрос вне СУЗ → оператору нет подсказки / низкий %; без автоответа клиенту | FR-CC-06, FR-UND-18 | готово |\n| SUF-T-16 | Порог `min_relevance_clarify`: кнопки уточнения клиенту только при max % ≥ порога (§4.2.1.15) | FR-UND-15, UC-UND-03, FR-UND-14 | готово |\n| SUF-T-17 | Одинаковые реплики клиента подряд: настраиваемое действие (§4.2.1.16) | FR-UND-16, UC-UND-04 | готово |",
    ),
    # Appendix D: close Pekarskaya comments
    (
        "| [42], [43] | ПЕК | FR-UND-12/13 §4.2.1.12–13: роль «Администратор базы знаний LLM» — дословно §2.4 п.2 | II.2 | FR-UND-12, FR-UND-13 | закрыто текстом |",
        "| [42], [43] | ПЕК | FR-UND-12/13 §4.2.1.12–13: роль «Администратор базы знаний LLM» — дословно §2.4 п.2 | II.2 | FR-UND-12, FR-UND-13 | закрыто текстом |\n| [44] | ПЕК | FR-UND-15 §4.2.1.15: описание не соответствует пункту ТТ (был журнал обучения вместо порога уточняющих вопросов) | II.2, II.7 | FR-UND-15, UC-UND-03, SUF-T-16 | закрыто текстом |\n| [45] | ПЕК | FR-UND-16 §4.2.1.16: описание не соответствует пункту ТТ (была модерация вместо одинаковых реплик подряд) | II.2, II.7 | FR-UND-16, UC-UND-04, SUF-T-17 | закрыто текстом |\n| [47] | ПЕК | Пункты 4.2.1.15, 4.2.1.16, 4.2.1.17 — где описание? | II.2, II.7 | FR-UND-15…17, UC-UND-03…05, SUF-T-16…17 | закрыто текстом |",
    ),
]

for old, new in replacements:
    if old not in text:
        raise SystemExit(f"MISSING pattern:\n{old[:120]}...")
    text = text.replace(old, new, 1)

path.write_text(text, encoding="utf-8")
print("OK: patched", path)
